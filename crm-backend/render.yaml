services:
  - type: web
    name: crm-backend
    runtime: java
    rootDir: crm-backend
    buildCommand: ./mvnw clean install -DskipTests
    startCommand: java -jar target/crm-backend-1.0.0.jar
    plan: free